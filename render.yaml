services:
  - type: web
    name: wernerburger-backend
    env: node
    plan: free
    buildCommand: "cd server && npm install && npm run build"
    startCommand: "cd server && npm start"
    envVars:
      - key: NODE_ENV
        value: production
