services:
  - type: web
    name: wernerburger
    env: node
    region: oregon
    plan: free
    buildCommand: npm ci
    startCommand: npm run start:server
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
